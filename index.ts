// Root entry for hosting platforms that auto-detect an entry file at the
// project root (e.g. Vercel's Express preset). Bootstraps the Express server
// exactly like `pnpm dev` / `pnpm start` do.
import "./server/_core/index";
